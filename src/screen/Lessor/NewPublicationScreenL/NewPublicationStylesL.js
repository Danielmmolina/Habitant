import { StyleSheet } from "react-native";

//Crear estilos solo relacionado con la pagina de arriendo

export const stylesNewPublicationScreen = StyleSheet.create({

    textTitle:{
       fontSize: 20,
       color: 'rgb(255, 255, 255)',
       textAlign: 'center',
       marginTop: '10%', 
    },
    

});
