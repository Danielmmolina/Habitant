import { StyleSheet } from "react-native";

export const HelpStyles = StyleSheet.create({

    containerInfo:{
        flexDirection: "row",
        margin: 10,
        alignItems: "center",
    }

});